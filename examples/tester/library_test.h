#ifndef LIBRARY_TEST_H
#define LIBRARY_TEST_H

#include <string>

// Function to run all library tests (GLFW, stb_image, GLM, and Assimp)
void runLibraryTests(const std::string& resourcePath);

#endif  // LIBRARY_TEST_H
